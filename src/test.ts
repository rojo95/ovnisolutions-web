// This file is required by the unit-test builder and loads the Zone.js testing
// environment. The test environment initialization is handled automatically
// by the unit-test builder (Vitest), so `initTestEnvironment` calls are not needed.

import 'zone.js';
import 'zone.js/testing';
